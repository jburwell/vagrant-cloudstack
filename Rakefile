# Initial structure borrowed from vagrant-aws and based on
# the Vagrant plugin development guide (http://docs.vagrantup.com/v2/plugins/packaging.html)

require 'rubygems'
require 'bundler/setup'
require 'rspec/core/rake_task'

# This installs the tasks that help with gem creation and
# publishing.
Bundler::GemHelper.install_tasks

